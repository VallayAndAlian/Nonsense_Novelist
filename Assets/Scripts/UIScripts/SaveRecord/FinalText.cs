using System.Collections;
using System.Collections.Generic;
using UnityEngine;
 [CreateAssetMenu(fileName = "TextList", menuName ="GameTextRecord/SaveList")]
public class FinalText : ScriptableObject
{
    public List<TextRecord> textList = new List<TextRecord>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
