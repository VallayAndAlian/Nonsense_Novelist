
// battle visual function

using UnityEngine;

public static class BVHelper
{
    public static Vector3 LogicToVisual(BattleUnitPos pos)
    {
        var runner = BattleRunner.Instance;
        if (runner != null)
        {
            
        }
        
        return new Vector3();
    }
}